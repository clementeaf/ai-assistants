# Copy to .env and set real secrets.
# IMPORTANT: do not commit real API keys.

# --- Core ---
# Optional: enables API-key auth for /v1 endpoints (format: projectA:keyA,projectB:keyB)
# AI_ASSISTANTS_API_KEYS=dev:dev-key

# --- LLM (OpenAI-compatible chat completions) ---
# For OpenAI:
AI_ASSISTANTS_LLM_BASE_URL=https://api.openai.com
AI_ASSISTANTS_LLM_API_KEY=sk-your-key
AI_ASSISTANTS_LLM_MODEL=gpt-4o-mini
AI_ASSISTANTS_LLM_TIMEOUT_SECONDS=15

# --- Intelligence-first: Domain planners (tool-calls) ---
AI_ASSISTANTS_PURCHASES_PLANNER_ENABLED=1
AI_ASSISTANTS_BOOKINGS_PLANNER_ENABLED=0
AI_ASSISTANTS_CLAIMS_PLANNER_ENABLED=0

# Optional: LLM router (domain classification)
AI_ASSISTANTS_LLM_ROUTER_ENABLED=1

# Optional: NLG rewriter (stylistic rewrite with guardrails)
AI_ASSISTANTS_LLM_NLG_ENABLED=0
AI_ASSISTANTS_LLM_NLG_STRICT=1

# --- Autonomous LLM mode (bypasses domain routing) ---
AI_ASSISTANTS_AUTONOMOUS_ENABLED=0
AI_ASSISTANTS_AUTONOMOUS_MAX_HISTORY=10

# --- Vector memory embeddings (OpenAI-compatible /v1/embeddings) ---
AI_ASSISTANTS_VECTOR_MEMORY_ENABLED=1
# For OpenAI, embeddings use the same base_url and api_key by default (fallbacks).
# You must set a model to enable embeddings:
AI_ASSISTANTS_EMBEDDINGS_MODEL=text-embedding-3-small
AI_ASSISTANTS_EMBEDDINGS_TIMEOUT_SECONDS=15

# --- Persistence ---
# AI_ASSISTANTS_SQLITE_PATH=.data/ai_assistants.sqlite3


